apiVersion: apps/v1
kind: Deployment
metadata:
  name: resolver-proxy
  namespace: resolver-apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resolver-proxy
  template:
    metadata:
      labels:
        app: resolver-proxy
    spec:
      containers:
        - name: proxy
          image: resolver-proxy:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
          env:
            - name: REDIS_URL
              # Refers to the service in the same namespace
              value: "redis://redis-service:6379"
          ports:
            - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: resolver-proxy-svc
  namespace: resolver-apps
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: resolver-proxy